package cz.ladicek.annDocuGen.example;

/**
 * A simple service that doesn't depend on anything and doesn't need any properties. It's used to demonstrate
 * the behavior in case a class has no {@code @Inject} nor {@code @Property} annotations and yet it's referenced
 * from somewhere.
 */
public class SimpleService {
}
